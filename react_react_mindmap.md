# React 核心知识体系 (详解与示例版)

- 核心基础
    - JSX 语法
        - 本质是 React.createElement
            - *示例*: `<div id="box">Hi</div>` 会被编译为 `React.createElement('div', { id: 'box' }, 'Hi')`
        - 条件渲染 (&&, 三元)
            - *示例*: `{isLogin ? <Profile /> : <Login />}` 或 `{unread > 0 && <span>有新消息</span>}`
        - 列表渲染 (key 的重要性)
            - *示例*: `{list.map(item => <li key={item.id}>{item.name}</li>)}`。`key` 帮助 React 在 Diff 算法中识别节点移动，避免过度销毁重建。
    - 组件化设计
        - 函数组件 vs 类组件
            - *示例*: 现代推荐函数组件 `const App = () => <div>Hello</div>`。类组件需继承 `React.Component` 并写 `render()`。
        - 组合胜过继承 (Composition)
            - *示例*: 通过 `children` 传入内容。`<Card> <h1>标题</h1> </Card>`，在 Card 内部渲染 `{props.children}`，解耦强关联。
        - 受控 vs 非受控组件
            - *示例*: 受控借由 state 驱动 `<input value={text} onChange={e => setText(e.target.value)} />`；非受控由 DOM 自己管理，通过 ref 获取 `<input ref={inputRef} />`。
    - 数据流
        - Props (单向数据流, Read-only)
            - *示例*: 父传子 `<Child title="标题" />`。子组件内部绝对不能修改 `props.title`。
        - State (组件自有的状态)
            - *示例*: `const [count, setCount] = useState(0)`。State 是局部的，改变它会触发重新渲染。
- Hooks 大全
    - 常用 Hooks
        - useState (状态管理)
            - *示例*: `const [user, setUser] = useState({ name: 'Alice' })`，用于管理页面需要响应的临时变量。
        - useEffect (副作用/生命周期)
            - *示例*: `useEffect(() => { fetchData() }, [id])`，当 `id` (依赖项) 变化时重新拉取数据。
        - useContext (跨层级通信)
            - *示例*: `const theme = useContext(ThemeContext)`。避免 props 层层钻取传递（Props Drilling）。
    - 性能优化 Hooks
        - useMemo (缓存昂贵计算结果)
            - *示例*: `const sortedList = useMemo(() => heavySort(list), [list])`，只有 `list` 变化才重新排序，防止每次渲染卡顿。
        - useCallback (缓存函数引用)
            - *示例*: `const handleClick = useCallback(() => submit(id), [id])`，防止子组件因为收到新函数（地址变更）而发生无效重渲染。
    - 引用与 DOM
        - useRef (持久化变量/DOM操作)
            - *示例*: `const inputRef = useRef(null); inputRef.current.focus()`。修改 ref 的 current 不会触发重新渲染响应。
        - useImperativeHandle (透传能力)
            - *示例*: 配合 `forwardRef`，让父组件可以调用子组件内部的方法，如表单组件暴露的 `ref.current.validate()`。
    - 高级与实验性
        - useReducer (复杂状态机)
            - *示例*: `const [state, dispatch] = useReducer(reducer, initialState)`。适合包含多个子值的复杂业务逻辑收敛。
        - useTransition (并发渲染)
            - *示例*: `const [isPending, startTransition] = useTransition(); startTransition(() => setList(hugeList))`。将耗时状态更新标记为低优先级。
        - useDeferredValue (推迟更新)
            - *示例*: `const deferredQuery = useDeferredValue(query)`。高优先级的输入框现行渲染，搜索结果的耗时列表延迟响应。
- 底层原理
    - 虚拟 DOM
        - *解释*: 用 JS 对象描述真实 DOM 的层级结构，经过对比后再更新，减少直接操作昂贵真实 DOM 的次数。
    - Fiber 架构
        - 双缓存机制
            - *解释*: 内存中保留两棵树（current 和 workInProgress），在内存中构建，构建完成后直接替换指针，不影响屏幕渲染。
        - 可中断更新与任务优先级
            - *解释*: 将大块渲染任务切片（Time Slicing）。遇到高优先级交互（打字、点击）可以暂停渲染，先响应用户操作。
    - 调和算法
        - Diff 算法 (Tree/Component/Element 层级)
            - *解释*: 设计原则为“同层比较不跨级”。组件类型不同直接销毁重建。元素类型相同只更新不同属性。
    - 合成事件
        - 统一接口与事件委托机制
            - *解释*: 所有的点击等事件并非绑定在被点击的 DOM 叶子节点上，而是委托到了顶层根节点，节省内存开销。
- 状态管理
    - 组件本地
        - useState / useReducer
            - *示例*: 单个组件内的下拉框展开/收起状态（UI State）。
    - 跨组件通信
        - Context API
            - *示例*: 整个 App 的 Light/Dark 主题切换，或当前已登录的用户权限信息。
    - 外部全局库
        - Redux Toolkit
            - *解释*: 适合超大型复杂状态，有统一的 Store 和严格单向通信流模式，便于时间旅行调试。
        - Zustand (主推/轻便)
            - *示例*: `const useStore = create((set) => ({ bears: 0 }))`。没有 Provider 嵌套“俄罗斯套娃”，上手极快。
        - Jotai / Recoil
            - *解释*: 原子化状态管理系统，将 state 打碎为独立小原子（Atom），修改原子精确定点重渲染组件。
    - 服务端状态管理
        - SWR / React Query
            - *示例*: 读取 API 数据时，自动接管 loading、error、请求去重、定期轮询和缓存（Server State 管理最佳实践）。
- 样式与 UI
    - CSS 方案
        - CSS Modules
            - *示例*: `import styles from './style.module.css'; <div className={styles.box}>`，通过编译成加哈希的类名彻底解决全局冲突。
        - Tailwind CSS
            - *示例*: `<div className="flex bg-blue-500 text-white rounded">`，使用原子类名拼装，专注结构渲染。
        - Styled-components
            - *示例*: `const Btn = styled.button`color: red`;`，这被称为 CSS-in-JS 方案。
    - 基础组件规范
        - Radix UI (无样式核心)
            - *解释*: 专注底层行为与极高的可访问性（a11y），不提供具体样式，让你自由发挥外观。
        - Shadcn UI (UI 基石)
            - *解释*: 近年最火的将 Radix 的无样式核心与 Tailwind 结合而成的“可复制粘贴式”组件库，你的项目 jushi 也在用它。
- 生态与工程
    - 路由管控
        - Next.js App Router
            - *解释*: React 官方首推的全栈架构方案。直接基于文件目录（app/page.tsx）自动生成支持 RSC（服务器组件） 的路由栈。
        - React Router
            - *解释*: 传统的纯客户端 SPA (单页应用) 路由跳转标准方案。
    - 性能与监控
        - Lighthouse / Core Web Vitals
            - *解释*: 谷歌制定的三大 Web 核心交互体验指标。LCP (最大内容绘制), CLS (累积布局偏移), INP (交互到下一次绘制)。
        - DevTools Profiler
            - *解释*: React 开发者工具插件。常用来记录和找出“哪个组件渲染最耗时（Render/Commit phase）”的性能瓶颈。
    - 测试工具
        - Vitest / Testing Library
            - *示例*: `render(<App />); expect(screen.getByText('Hello')).toBeInTheDocument();` （组件级视图测试）
- 版本演进 (18 -> 19)
    - React 18 特性
        - 自动批处理 (Batching)
            - *解释*: 在 Promises、setTimeout 或是各种事件回调中连续调用 `setA(); setB();` 时，只会合并触发 1 次渲染。
        - Suspense (流式渲染)
            - *解释*: 用 `<Suspense fallback={<Loading/>}> <AsyncComp />` 优雅地展示 fallback。结合流式 HTML SSR 极快展示无数据首屏。
    - React 19 特性
        - Actions (useActionState)
            - *示例*: `const [error, submitAction, isPending] = useActionState(fetchAPI, null)`。原生级完美接管表单的 isLoading(isPending) 状态反馈。
        - Ref 简化 (不再强制 forwardRef)
            - *示例*: 定义组件 `function Input({ref, ...props})` 即可，`ref` 像普通 prop 一样直接传递，干掉样板代码。
        - use() Hook (异步处理)
            - *示例*: `const data = use(fetchPromise);`，打破了“Hooks不能写在条件判断里”的限制，并可在渲染期直接挂起 promise。
        - 服务器组件 (RSC - Server Components)
            - *解释*: 你的 `page.tsx` 默认在服务端跑完并渲染成纯原生 HTML 格式发送给客户端，达到 **0 JavaScript Bundle Size**，全面减负客户端。
