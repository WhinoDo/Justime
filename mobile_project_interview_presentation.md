# 聚识 (Justime) 移动端项目 - 面试展示指南

这是一份为你准备的面试展示指南。在向面试官展示移动端项目时，你可以遵循以下流程，从整体架构、登录流程，到核心的聊天和日程功能，结合代码中的技术亮点进行讲解。

---

## 1. 项目整体架构与工程化 (开场介绍)

**🗣️ 展示话术：**
> “面试官您好，这是我开发的聚识移动端 App。项目基于 **React Native** 和 **Expo** 框架构建，全程使用 **TypeScript** 保证类型安全，路由管理使用了最新的 **Expo Router**。在架构设计上，我非常注重逻辑与 UI 的分离，以及各种状态的精细化控制。”

**💻 技术细节与亮点：**
- **UI 与业务逻辑分离**：以聊天页面 (`ChatScreen`) 为例，剥离出了 `useChatScreenLogic` 自定义 Hook。UI 组件只依靠 Hook 返回的状态 (`logic.loading`, `logic.error`, `logic.auth`) 进行渲染，使得页面组件极其纯粹、易于维护。
- **主题化设计系统**：通过 `ThemeProvider` 和自定义的 `useThemeColor` Hook，应用实现了在浅色 / 深色模式下的丝滑切换。抽象出 `ThemedText` 等原子组件，保证了全 App 的设计一致性。
- **本地开发优化 (Monkey Patch)**：为了解决跨网穿透工具 (如 loca.lt) 频繁出现警告页面的问题，在入口 `_layout.tsx` 中巧妙地 **Monkey patch** 拦截了 `global.fetch`，自动在请求头带上 `Bypass-Tunnel-Reminder`，扫清了真机调试时的网络障碍，体现了极强的工程化解决问题能力。

---

## 2. 鉴权与登录流程 (Auth Module)

**🗣️ 展示话术：**
> “在打开 App 时，如果没有 Token，会出现一个基于 Overlay 设计的鉴权弹窗。这里我实现了一套完整的 JWT 登录与状态持久化机制。”

**💻 技术细节与亮点：**
- **状态持久化与水合 (Hydration)**：基于 `AsyncStorage` 缓存用户的 `token` 和 `user` 信息。在 `AuthContext` 初始化时，通过异步的水合逻辑（`hydrate`）恢复登录状态。为了防止极端网络或磁盘 IO 阻塞，还做了一个 6 秒的 Fail-Safe 定时器兜底。
- **动态多后端联调机制**：考虑到多环境问题，在 `AuthOverlay` 中，我支持用户手动输入、切换 Backend Base URL。并且在发送任何网络请求前会自动 `normalizeBaseUrl`，清洗用户手抖多输入的 `/`，提高了系统的鲁棒性。
- **安全的 User 对象解析**：对于缓存中反序列化的 User 信息，没有盲目相信 JSON.parse，而是实现了一个 `safeParseUser` 纯函数对属性和类型进行防御性校验，防止因为脏缓存导致 App 崩溃。

---

## 3. AI 聊天功能与任务流 (Chat Feature)

**🗣️ 展示话术：**
> “登录后，默认进入的是核心的 AI 聊天界面。这是我下功夫最深的模块，不仅处理了复杂的软键盘交互，还深度整合了 AI大模型的结构化输出。当用户让 AI 帮忙规划事情时，系统能够直接将对话转化为可视化的任务卡片。”

**💻 技术细节与亮点：**
- **软键盘与布局自适应**：精心调优了 `KeyboardAvoidingView` 的 `keyboardVerticalOffset`，结合底部的 `useBottomTabBarHeight()`，确保键盘弹起时完美贴合输入框，且不遮挡历史消息。
- **结构化卡片 - TaskDecompositionView (高阶组件)**：
  - 当 AI 分析完用户意图后，会在聊天流中渲染一个 `TaskDecompositionView`（任务分解建议卡片）。
  - **性能与状态控制**：组件内部使用了大量的 `Set` 结构（`selectedTasks`, `expandedTasks`, `addedTasks`）在 O(1) 的时间复杂度下来追踪每个任务子项的状态，非常高效。
  - **批量处理与容错逻辑**：实现了**一键全选并加入日程**功能 (`addTasksByIndexes`)。采用了 `Promise` 机制串行/并行调用外部的 `onAddTask`，就算加入多个任务时部分失败了，界面也能清晰给出错误提示（“已添加 X 个，失败 Y 个”），保留已成功任务的样式，容错处理做得很精细。

---

## 4. 强大的日程管理功能 (Schedule Feature)

**🗣️ 展示话术：**
> “刚刚通过 AI 规划加入的任务，都会流转到日程模块。在这个模块，我基于 `react-native-calendars` 进行了深度的定制化改造，不仅支持常见的列表流，更是手写实现了一个复杂的时间轴碰撞算法。”

**💻 技术细节与亮点深度剖析 (面试官必问点)：**

- **自定义时间轴布局算法 (核心竞争力) 🌟**：
  *面试官常常会问：“遇见过复杂的 UI 需求怎么处理？”这个点是展示你算法落地能力的最佳场景。*
  在日视图的时间轴 (Timeline) 模式下，为了解决多日程时间重叠导致的视觉混叠问题，我没有妥协于第三方的重型日历库，而是**手写实现了一个二维空间碰撞检测与布局算法** (`calculateEventLayout`)。核心分三步：
  1. **排序 (Sorting)**：首先将当天所有事件按 `start` 升序排列，相同时间则按 `end` 降序。
  2. **聚类分块 (Clustering)**：遍历事件，维护一个动态的区块边界(游标)。只要新事件的开始时间早于当前区块内最晚的结束时间，意味着它们在时间轴上“碰撞”了，即归入同一个“重叠连通图 (Cluster)”；否则新开一个 Cluster。这精妙地将全局布局降维为了局部重叠运算。
  3. **列宽与偏移推导 (Column Allocation 贪心算法)**：针对每个 Cluster，维护一个 `columns` 数组（记录每一列当前被占用的最晚结束时间）。如果一个事件能在原有列中放下（即 `eventStart >= columnEnd`），则更新该列最晚时间；放不下就 push 新开一列。这计算出了该组的重叠深度（即最大并发列数）。最终事件的水平宽度比 (`widthRatio`) 即为 `1 / 列数`，水平偏移 (`leftRatio`) 即为 `它的列索引 * widthRatio`。
  *总结：这段逻辑仅仅用 $O(N \log N)$ 的时间复杂度，彻底抹平了组件由于重叠渲染而导致的排版错位问题。*

- **轻量级交互与动画体验 (LayoutAnimation)**：
  在客户端开发中，流畅度是第一生命。在切换日历日期引起视图高度自适应、由普通的列表视图切换至时间轴（`toggleViewMode`）、展开或闭合详情卡片时，我全局使用了原生层驱动的 `LayoutAnimation.configureNext`。
  *亮点*：其设计哲学在于，它免去了引入 `Reanimated` 去手动管理大量 SharedValue 与繁重 Worklet 脚本的开销。无需显式定义动画插值曲线，在一行配置代码之后直接调用 React 的 `setState`，React Native 底层 C++ 引擎即会接管新老虚拟 DOM 树的坐标空间差异，自动计算并执行插值过渡，达到了零帧丢失、极致轻量的丝滑推拉体验。

- **状态管理：规避全量刷新的内存原位替换策略**：
  *面试官关注点：数据发生变更后界面怎么响应是最快且无性能损耗的？*
  - **局部更新取代全量刷新**：传统思维在“修改保存日程”后 (`handleUpdateEvent`) 会简单粗暴地触发全局 `loadEvents`。这对于带有日历组件和长列表的视图而言是极其消耗性能的，会引发整个界面的重绘和跳帧，甚至丢失用户当前的页面滑动位置。
  - **混合更新模型**：我设计了一套机制：发起网络 `PUT` 同步请求，等待接口响应以保障服务端数据已安全落库（悲观保证数据一致性）；拿到服务端下发的单体最新 Event 数据反序列化后，在客户端直接使用 `setEvents(prev => prev.map(item => item.id === eventId ? updatedEvent : item))` **对 React state 原数组中唯一的对应项进行内存替换**。使得 FlatList 可以根据对象的引用地址变化，极其轻量化地局部重新渲染对应卡片，交互做到了绝对的无缝丝滑。
  - **避免数据污染的边缘副作用清理**：在批量删除模式这一高危操作中，针对某些极端边缘场景（如用户先在今天勾选了未删除项，又中途横向滑动切换到了翌日日历），我利用 `useEffect` 进行精密的副作用拦截，动态比对当日的实际可见日程数据和装载在 `Set` 中的缓存集合，自动过滤与摘除掉已不在可视范围的非法脏 ID，在根源上杜绝了对它日数据越权与误删的潜在灾难级 Bug。

- **健壮的复杂表单封装与边界防御 (EventDetailSheet)**：
  日程的详情与编排涵盖了事件输入、时间控件以及动态多模态资源挂载。
  - **时区与非法日期防御编程**：面对移动双端 Date 选择器行为不一致的情况，自研了受控的时间组件交互。核心引入了自研的 `clampPickerStateDay` 算法，用来收敛和抹平日历越界异常，能防患于未然地将类似“用户强行手选 2月31日”这类的越轨行为，柔性向后回退至 28 日，实现健壮的闭环纠错。
  - **沙盒外部环境跳转的安全接管**：针对日程挂载跳转 App 外部第三方资料连接（Resources）的设计诉求。在代码执行层面，禁绝了无脑调用 `Linking.openURL` 的粗暴行为。而是在协议跳转分发前，强制织入一层由 `Linking.canOpenURL` 把关的端侧沙盒子系统的合法性甄别。避免了特定的 Schema 伪造，亦或者由于物理设备上因匮乏相关宿主应用程序所导致的宿主 App 本身 Native Crash，展示了企业级应用的抗压求稳考量。

---

## 5. 总结陈词 (Closing)

**🗣️ 展示话术：**
> “总的来说，这个移动端项目不仅仅是用 React Native 搭了一个壳，而是在细节上、状态流转上以及边界异常捕获上都按照企业级生产标准去落地的。从自动规避跨域代理页，到结构化的 AI 交互，再到复杂的图表时间轴碰撞算法，这些都是我在解决具有一定复杂度问题时的验证。谢谢您的查看！”
