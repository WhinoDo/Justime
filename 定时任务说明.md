# 🕐 定时任务配置完成

## ✅ 已创建的脚本文件

| 文件 | 用途 | 频率 |
|------|------|------|
| `scripts/cron/health_check.py` | 健康检查（后端、数据库、磁盘） | 每 5 分钟 |
| `scripts/cron/backup_db.py` | MongoDB 数据库备份 | 每天凌晨 2 点 |
| `scripts/cron/cleanup_temp.py` | 清理临时文件和日志 | 每天凌晨 3 点 |
| `scripts/cron/code_optimizer_agent.py` | 启动子 Agent 优化代码 | 每天凌晨 4 点 |
| `scripts/cron/install_crontab.sh` | 一键安装定时任务 | 手动执行 |
| `scripts/cron/crontab.example` | Crontab 配置模板 | - |
| `scripts/cron/README.md` | 使用说明文档 | - |

## 🤖 代码优化 Agent 说明

`code_optimizer_agent.py` 会启动一个子 Agent 自动执行：

### 扫描内容
- **前端代码**: `jushi_agent/src/`
- **后端代码**: `jushi_backend/app/`

### 优化类型
| 类型 | 检查项 |
|------|--------|
| 性能优化 | N+1 查询、不必要重渲染、内存泄漏 |
| 安全问题 | SQL 注入、XSS 漏洞、硬编码密钥 |
| 代码质量 | 重复代码、过长函数、深层嵌套 |
| 最佳实践 | TypeScript 类型、React Hooks、Python 风格 |

### 工作流程
1. 扫描代码 → 2. 识别问题 → 3. 自动修复 → 4. 生成报告

## 🚀 安装方法

```bash
# 进入脚本目录
cd /Users/zhuyuxuan/Desktop/Code/jushi/scripts/cron

# 运行安装脚本
./install_crontab.sh

# 或手动添加 crontab
crontab crontab.example
```

## 📋 定时任务执行时间表

```
┌──────────────┬──────────────────────────────────────────┐
│ 时间         │ 任务                                     │
├──────────────┼──────────────────────────────────────────┤
│ */5 * * * *  │ 健康检查（每5分钟）                       │
│ */30 * * * * │ 代码优化 Agent（每30分钟）                │
│ 0 * * * *    │ 磁盘空间检查（每小时）                    │
│ 0 2 * * *    │ 数据库备份                               │
│ 0 3 * * *    │ 清理临时文件                             │
│ 0 4 * * 0    │ 日志压缩（周日）                          │
│ 0 5 * * 1    │ 代码深度优化（周一）                      │
└──────────────┴──────────────────────────────────────────┘
```

## 📁 输出目录

```
jushi/
├── logs/                        # 日志目录
│   ├── health_check.log
│   ├── backup.log
│   ├── cleanup.log
│   └── code_optimizer.log
├── backups/                     # 备份目录
│   └── mongodb_YYYYMMDD_HHMMSS.tar.gz
└── docs/
    └── optimization_reports/    # 优化报告
        └── optimization_report_YYYYMMDD.md
```

## 🧪 测试运行

```bash
# 测试健康检查
python3 scripts/cron/health_check.py

# 测试代码优化 Agent
python3 scripts/cron/code_optimizer_agent.py
```

## ⚙️ 常用命令

```bash
# 查看已安装的定时任务
crontab -l

# 编辑定时任务
crontab -e

# 移除所有定时任务
crontab -r

# 查看 cron 日志（macOS）
log show --predicate 'process == "cron"' --last 1h
```
